apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: backend
spec:
  ports:
  - name: http-8080
    port: 8080
    targetPort: 8080
  selector:
    deployment: backend
  type: ClusterIP
  sessionAffinity: None    